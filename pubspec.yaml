name: quantum_stability_lab
description: Quantum Stability Lab - اتفاق اور قانون کا امتزاج
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # صرف essential packages رکھیں
  http: ^0.13.5
  provider: ^6.0.0
  path_provider: ^2.0.15
  csv: ^5.0.1
  share_plus: ^7.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
