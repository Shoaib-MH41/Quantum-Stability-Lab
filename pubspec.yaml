name: quantum_stability_lab
description: Quantum Stability Lab - اتفاق اور قانون کا امتزاج
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # نیٹ ورک اور سینسرز
  http: ^0.13.5
  provider: ^6.0.0
  connectivity_plus: ^5.0.2  # یہ لائن شامل کر دی گئی ہے
  sensors_plus: ^4.0.2
  battery_plus: ^5.0.1
  vibration: ^1.8.4

  # ڈیٹا مینجمنٹ
  path_provider: ^2.0.15
  csv: ^5.0.1
  share_plus: ^7.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
