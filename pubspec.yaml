name: quantum_stability_lab
description: Quantum Stability Lab - اتفاق اور قانون کا امتزاج
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Sensor Integration
  sensors_plus: ^2.0.0
  battery_plus: ^3.0.3
  connectivity_plus: ^5.0.1
  device_info_plus: ^10.0.0
  vibration: ^1.7.4
  
  # Charts & Visualization
  fl_chart: ^0.60.0
  
  # State Management
  provider: ^6.0.0
  
  # Network & HTTP
  http: ^0.13.5
  
  # Data Export & Storage
  csv: ^5.0.1
  path_provider: ^2.0.15
  share_plus: ^7.0.2
  
  # UI Enhancements
  shimmer: ^3.0.0
  animated_text_kit: ^4.2.2
  
  # Utilities
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

